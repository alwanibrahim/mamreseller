services:
  mamqamify:
    build: .
    container_name: mamqamify
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file: .env
    environment:
      PORT: "3001"
    volumes:
      - ./konektor.db:/app/konektor.db
